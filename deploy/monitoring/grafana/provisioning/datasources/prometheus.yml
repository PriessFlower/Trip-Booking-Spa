apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: spa-prom
    access: proxy
    # Resolved over the compose network, so it does not depend on the host address.
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 30s
