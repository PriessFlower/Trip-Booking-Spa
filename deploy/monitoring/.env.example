# Copy to /opt/monitoring/.env on the host, mode 0600, and set a generated password.
# The real file is never committed; it holds the only Grafana admin credential.
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=replace-me
