# trip-booking-spa 开发规范

## 0. 关于本文档

**0.1 规范性措辞**　本文档使用下列措辞表达约束强度：

| 措辞 | 含义 |
|---|---|
| **必须** / **禁止** | 强制条款。违反视为缺陷，评审应当拒绝。 |
| **应当** / **不应** | 推荐条款。除非有充分理由并在评审中说明，否则遵循。 |
| **可以** | 许可条款。由实施者自行判断。 |

**0.2 条款引用**　条款按 `§章.节` 编号。评审意见与提交信息中可直接引用，例如「违反 §1.2.5」。

**0.3 准入标准**　新增条款必须满足：该规则若不成文，本项目确实存在被违反的可能。仅属通用最佳实践、无本项目具体背景的内容，禁止写入本文档。

---

## 1. 提交规范

### 1.1 首行

**1.1.1**　提交信息首行必须符合下列格式，其中 `(<scope>)` 可省略：

```
<type>(<scope>): <subject>
```

**1.1.2**　`type` 必须取自下表，禁止自造：

| type | 适用范围 |
|---|---|
| `feat` | 新增能力 |
| `fix` | 修复缺陷 |
| `refactor` | 不改变外部行为的重构、收口、归位、简化 |
| `chore` | 删除死代码、调整依赖及其他杂务 |
| `ci` | 流水线、部署脚本与相关配置 |
| `docs` | 文档 |
| `test` | 仅变更测试代码 |

**1.1.3**　`scope` 可选，取值为供应商名或模块名，必须小写。跨模块或全局性改动应当省略 `scope`，禁止为凑格式而填写无意义的值。

现有取值：`expedia`　`ratehawk`　`meituan`　`huitravel`　`didatravel`　`aichotels`　`fastpay`　`amap`　`cache`　`task`　`ratelimit`　`catalog`　`geo`　`db`　`deploy`

新增取值可以直接使用，不需预先登记；但应当复用已有取值，避免同义词泛滥（例如已有 `cache` 就不应再用 `caching`）。

**1.1.4**　`subject` 必须使用英文祈使句，即动词原形开头（`add`，而非 `added` 或 `adds`）。

**1.1.5**　`subject` 首字母必须小写，句末禁止加句号，整行长度不应超过 72 字符。

**1.1.6**　首行必须描述**做了什么**，禁止仅写模块名或含糊表述（如 `fix(expedia): bugfix`、`refactor: cleanup`）。

### 1.2 正文

**1.2.1**　正文必须使用中文。首行与正文之间必须空一行。

**1.2.2 读者基准**　正文的预期读者是**未读过本次改动所涉代码的同事**。正文必须让这样的读者一遍读懂，禁止写成仅本人可解的备忘。

**1.2.3 结构**　除首行已足以自明的极简改动外，正文应当按「问题 / 原因 / 改动 / 验证」分段，无内容的段可省略。缺陷修复必须包含「问题」与「原因」两段。

**1.2.4 顺序**　必须先陈述**可观察到的影响**（什么坏了、影响多大），再展开技术原因。禁止以内部标识符或代码结构开篇。

**1.2.5 术语**　首次出现的字段名、内部概念应当以括号附简短解释，例如 `last_time（上次刷价时间）`。

**1.2.6**　因果链超过两层时，应当拆句陈述，并在末尾用一句话点明净结果，禁止将多层因果压入单句。

**1.2.7**　已完成运行时验证的变更，正文应当附上验证方式与观察到的结果。

**1.2.8**　破坏性变更必须在正文中以 `BREAKING CHANGE:` 开头的独立段落说明影响与迁移方式。

**1.2.9**　禁止添加 `Co-Authored-By` 等署名尾注。

### 1.3 示例

**符合规范**

```
fix(expedia): guard null last_time in price refresh task

问题：刷价定时任务完全不工作，日志狂刷空指针，一条价格都刷不出来。

原因：任务表中新加入的酒店，last_time（上次刷价时间）字段为空，代码直接
拿它做日期比较，触发空指针。该异常又抛在「单行处理」的 try 之外，被最外层
while(true) 捕获后立即整轮重跑，于是卡在这一行反复失败、持续空转压库。
即：只要清单里有一个新酒店，整个刷价任务就瘫痪。

改动：
- last_time 与 upgrade_deadline 补空值保护，空值分别视为「非同一天」「已过期」
- 单行处理整体移入 try，坏数据只跳过该行，不再拖垮整轮

验证：用同一条 last_time 为空的任务行重跑，空指针与循环重启均为 0 次，
Redis 正常写入 price / product 键。
```

```
chore: remove unused RateLimiterUtils

限流已统一收口至 RateLimitManager，该工具类全库零引用。
```

**不符合规范**

| 反例 | 问题 |
|---|---|
| `修复：刷价任务崩溃` | 首行非英文（违反 §1.1.1、§1.1.4） |
| `fix(expedia): fixed the NPE.` | 非祈使句、句末有句号（违反 §1.1.4、§1.1.5） |
| `refactor: cleanup` | 未说明做了什么（违反 §1.1.6） |
| `fix(price-cache-refresh-task): ...` | scope 过细且与已有 `cache`／`task` 重复（违反 §1.1.3） |
| 仅有首行、改动涉及行为变更 | 缺少问题与原因说明（违反 §1.2.3） |
| 正文以 `isSameDay 与 after() 直接 NPE…` 开篇 | 以内部标识符开篇，未先说明影响（违反 §1.2.4） |
| `新任务行的 last_time 天生为空，异常抛在单行 try 之外被外层兜住后立即重跑导致空转` | 三层因果压入单句，且术语无解释（违反 §1.2.5、§1.2.6） |

### 1.4 提交时机

**1.4.1**　变更未经人工确认，禁止自动提交。

**1.4.2**　一次提交应当只表达一个意图。互不相关的改动必须拆分为多次提交。

---

## 2. 分支与发布

### 2.1 分支模型

```
                        ┌── hotfix/* ──┐          从 master 切，修完并回 master
                        │              ↓
master   ───────────────┴──────────────●────────  生产。合并即自动部署，无人工审批
  ↑                                    │
  │  攒够一波 → 测试验收 → PR 合入       │ 立即同步回 dev（§2.3.4）
  │                                    ↓
dev      ──●──────●──────●─────────────────────  集成分支
           ↑      ↑      ↑
        feat/*  fix/*  chore/*                   从 dev 切，改完并回 dev
```

**2.1.1**　分支必须以 `<type>/<描述>` 命名，`<type>` 取值同 §1.1.2；紧急修复用 `hotfix/`。

**2.1.2 常规开发**　一切非紧急改动（功能、缺陷、重构、杂务、文档、CI）均走此通道：分支必须**从 `dev` 切出**，PR 必须以 `dev` 为目标分支。

**2.1.3**　禁止从 `master` 切出常规分支。否则该分支缺少 `dev` 上已合入的改动，合回 `dev` 时易产生无谓冲突，也无法与其他在途改动相互验证。

**2.1.4 发布**　发布由 `dev` → `master` 的 PR 进行，开出前必须经 §2.2.3 的测试验收。

一批发布通常包含多个改动——`dev` 上自然会汇集若干在途改动，攒够一批再发是常态。但**批量并非强制**：单个改动即可构成一批，无须为凑数等待。是否再等取决于有无其他改动即将合入，以及本次改动的紧要程度。

**2.1.5**　禁止向 `master` 直接推送。禁止在本地 `master` 上提交。

**2.1.6**　分支合并后必须删除，本地与远程均需清理。

**2.1.7 评审**　以 `dev` 为目标的 PR **可以**评审；以 `master` 为目标的 PR（即发布与 hotfix）**应当**评审，因其直接影响生产。

### 2.2 CI 触发点

| 动作 | 执行内容 |
|---|---|
| push 到 `dev` 或功能分支 | 不触发（有意为之，避免无谓构建） |
| PR → `dev` | 不触发 |
| PR → `master` | 构建 + 测试 |
| push 到 `master`（即合并 PR） | 构建 + 测试 + 打镜像 + **部署生产** |

**2.2.1**　因上表所列触发点，`dev` 上的改动在开出 `dev` → `master` 的 PR 之前不经任何自动检查。**据此，向 `dev` 合并前必须已在本地实际运行过所改动的链路并观察其行为**，不得以「等 CI 兜底」为由跳过。编译通过与单元测试通过不构成验证。

**2.2.2**　合并至 `master` 即发布生产，无人工审批环节。合并前必须确认附录 A 所列上线前置条件均已满足。

**2.2.3 发布前测试验收**　`dev` → `master` 的 PR 开出前，必须对本批改动整体做一次测试验收——不是逐个改动各自验过即可，因为改动之间的相互影响只在合并后才显现。

验收范围至少覆盖：服务能正常启动、查价与验价链路可用、本批改动各自声称的行为确实生效。

> **当前缺口**：尚无独立测试环境（测试库、Redis、Nacos 均未搭建），故本条目前以**本地 `dev,local` 剖面整批实跑**替代。这是权宜之计，其局限是本地配置与生产不完全一致（如剖面差异、Nacos 命名空间、网络出口）。测试环境建成后，本条应改为在该环境验收。

### 2.3 紧急修复（hotfix）

**2.3.1 适用情形**　仅下列情形可走 hotfix 通道：

| 情形 | 判据 |
|---|---|
| 生产故障 | 生产环境当前不可用、行为错误或数据受损，修复不能等待 `dev` 汇集 |
| 部署链路故障 | 缺陷位于 CI 或部署脚本，致使无法完成部署 |

**2.3.2 必须从 master 切出**　hotfix 分支必须**从 `master` 切出**，PR 以 `master` 为目标。

**禁止从 `dev` 切出 hotfix**：`dev` 上通常含已合入但尚未发布的改动，从 `dev` 切出的修复合入 `master` 时会将这些改动一并带上生产，等于在故障处置期间夹带未经发布评估的变更。

**2.3.3 范围最小**　hotfix 只允许包含修复该故障所必需的改动。顺手的重构、清理、无关缺陷修复必须留待常规通道。

**2.3.4 立即同步回 dev**　合并至 `master` 后必须立即将 `master` 同步回 `dev`（`dev` 无独立提交时可快进），禁止令两分支分叉。否则后续 `dev` → `master` 的 PR 会与该修复冲突。

**2.3.5 说明义务**　hotfix 的 PR 描述必须写明所依据的情形及其判据。未写明者视为违反 §2.1.2。

---

## 3. 配置规范

### 3.1 判定顺序

**3.1.1**　新增一项设置前，必须先判断它**是不是配置**：

| 特征 | 类别 | 去向 |
|---|---|---|
| 由**人**写入，删除后无法自行恢复，需人重新填写 | 配置 | 按 §3.2 归位 |
| 由**程序**运行时写入，删除后程序可自行重新生成 | 数据 | Redis / MySQL，**不适用本章** |

**3.1.2**　Redis 与 MySQL 是数据存储，**不是配置载体**。禁止将人工维护的开关、名单、阈值类配置写入 Redis。

> 判据示例：删除 Redis 中的价格缓存，刷价任务下次运行即可补回，属数据；删除 Nacos 中的酒店缓存白名单，程序无从得知该缓存哪些酒店，只能等人重新填写，属配置。

### 3.2 载体归位

**3.2.1**　确认为配置后，必须按下表归位：

| 载体 | 承载内容 | 判据 |
|---|---|---|
| `application*.yml` | 数据源、Redis / Nacos 连接、各供应商接口地址、框架与技术参数 | **启动必需**——缺失则无法启动或无法连接 Nacos |
| Nacos<br>（Data ID `trip-booking-spa.yaml`，Group `DEFAULT_GROUP`） | 开关、名单、阈值、限流 QPS、佣金率 | **运维可调**——运行期需要调整且不应重启 |
| `application*.yml`（安全护栏） | 见 §3.2.3 | **误开后果不可逆**——变更必须经发版与评审 |

**3.2.2**　运维可调的配置项，其权威取值必须在 Nacos，且**只能**在 Nacos。

**3.2.3 安全护栏**　同时满足下列两条的开关归为安全护栏，**必须固定在 `application*.yml`，禁止移入 Nacos**：

1. 误开将造成资损、越权或其他不可逆后果；
2. 在启动阶段校验，取值不合法时拒绝启动（而非在业务路径上判定）。

设此类别的目的是让其变更必须经过发版与代码评审，不能由任何人在配置台单方面打开。现有成员：

| 开关 | 误开后果 |
|---|---|
| `expedia.booking-enabled` | 未经认证即向 Expedia 提交真实订单，产生真实费用 |
| `expedia.production-endpoint-enabled` | 连接 Expedia 生产端点，消耗真实配额并可能产生真实交易 |

**3.2.4**　运维开关与安全护栏易被混淆，归类时以 §3.2.3 的两条为准。**在业务路径上按次判定、且存在正当运维停用场景的，一律属运维可调**，例如 `supplier.expedia.static-data-enabled`（每次调用时判定，运维需要时应能停摄取而不发版）。

### 3.3 兜底默认值

**3.3.1**　运维配置**禁止**出现在任何 `application*.yml` 中，兜底默认值亦不例外。yml 一旦承载运维配置，各剖面便会各存一份，取值来源随即失去唯一性。

**3.3.2**　兜底默认值必须写在代码的 `@Value` 表达式内，与使用处同址：

```java
@Value("${supplier.expedia.static-data-enabled:false}")   // 默认关闭
private boolean staticDataEnabled;
```

**3.3.3**　兜底默认值必须取**安全侧取值**：开关默认关闭、限额默认从严、名单默认为空。禁止以生产实际值作为兜底默认值。

**3.3.4**　运行期的实际取值以 Nacos 为准。调整运维配置必须改 Nacos，禁止通过发布新版本修改代码中的默认值来变更线上行为。

### 3.4 剖面

**3.4.1**　`application*.yml` 的剖面差异**只应体现在基础设施上**（数据源地址、连接凭据来源、接口地址）。禁止因剖面不同而在 yml 中给出不同的业务行为取值。

**3.4.2**　任一 `application*.yml`（含 `-local`、`-dev`、`-prod`）均禁止包含与 Nacos 同名的运维配置键。同名键将覆盖 Nacos，导致**改 Nacos 不生效**。

**3.4.3**　在 §3.4.2 所述遗留同名键清理完毕前，本地验证 Nacos 取值必须改用命令行参数覆盖：

```bash
java -jar target/trip-booking-spa-0.0.1.jar --spring.profiles.active=dev,local \
  '--supplier.hotel.cache.map={"10005":["16024166","10970375"]}'
```

### 3.5 凭证

**3.5.1**　API 密钥、数据库口令等凭证禁止写入 `application*.yml`，亦禁止写入 Nacos，必须经环境变量注入。

**3.5.2**　新增凭证类变量时，必须在 `.env.example` 中登记变量名；`.env.example` 中禁止出现真实取值。

### 3.6 变更与同步

**3.6.1**　新增 Nacos 配置项时，应当同步更新 `config/nacos/trip-booking-spa.yaml.example`。

**3.6.2**　新增配置项时，代码侧 `@Value` 必须带默认值；确无合理默认值而要求启动即失败的，必须在该字段附注释说明原因。

### 3.7 键名规则

**3.7.1**　配置键必须符合 `<域>.<对象>.<属性>` 三层结构，禁止更深嵌套。

**3.7.2**　`<域>` 为封闭枚举，新增配置时按下表提问归位：

| 域 | 判定问题 | 示例 |
|---|---|---|
| `task` | 这是由定时任务驱动的吗？ | `task.expedia-cps.enabled` |
| `ratelimit` | 这是在控制调用外部接口的速率吗？ | `ratelimit.default-qps` |
| `cache` | 这是在控制**我方自己**的缓存吗？ | `cache.price.hotels` |
| `supplier` | 这是发给某供应商、或只对某家生效的吗？ | `supplier.didatravel.real-time-price` |

**3.7.3**　归不进上述四域时，方可新增顶层域，且必须先在本节表格登记。禁止未登记即启用新域。

**3.7.4**　命名词汇必须统一：`<对象>` 与 `<属性>` 使用 kebab-case；布尔开关以 `-enabled` 结尾；名单与集合使用复数；速率统一用 `qps`。

**3.7.5**　禁止在不同域间借用同一词汇表达不同含义。例如 `cache` 仅指我方缓存，供应商侧的缓存/实时取舍必须归入 `supplier` 域并另行命名。

**3.7.6**　完整键值清单以 `config/nacos/trip-booking-spa.yaml.example` 为唯一出处，本文档不重复列举，避免两处维护产生漂移。

### 3.8 闸口纪律

本节所称「闸口」指请求或任务执行前决定放行、拦截或降级的判断单元，包括配置开关、分布式锁、限流、熔断与名单。

**3.8.1 准入**　新增闸口前必须回答：**是否存在正当运维场景，需要不发版即关闭它？** 回答为否的，必须写死为业务规则，禁止上配置面。

**3.8.2 一事一闸**　同一行为只允许一个权威闸口。禁止将两个生命周期不同的开关（如启动期绑定值与热更新值）在同一判定点做与或运算。需要多来源取值时，必须在闸口内部按固定优先级归一，对外只暴露一个判定入口。

**3.8.3 关闸即停做功**　闸口判定必须置于所在路径的最外层。验收标准为「关闸后该路径的 CPU 占用与对象分配归零」，而非「功能不产生效果」。禁止存在关闸后仍执行循环、写库或对象复制的实现。

**3.8.4 拦截可定位**　闸口拦截请求时必须输出可检索日志，内容须包含闸口标识与业务主键（如酒店 ID、日期），级别不低于 `info`。**REJECT 分支没有可检索输出的闸口，视为未实现。**

**3.8.5 风险与执行面声明**　每个闸口必须在其判定逻辑所在文件内以注释声明三项：误开的后果、误关的后果、生效执行面（哪些 profile 或节点角色会执行它）。禁止另建外部备注，以防与代码漂移。

**3.8.6 禁止硬编码业务闸**　影响业务可用性的开关与名单禁止写死在代码或打包进制品，必须可经 Nacos 调整。

**3.8.7 限流分层**　限流允许且仅允许两层，新增限流必须归入其一，禁止第三层：

| 层 | 职责 | 失败方式 |
|---|---|---|
| 供应商配额层（全局，`ratelimit.*`） | 不超供应商契约配额 | 快速失败 |
| 调用方配额层（按调用方，如 `task.*.qps`） | 为在线流量保留余量，限制后台任务占用 | 阻塞等待 |

调用方配额必须小于对应的供应商配额，否则该层形同虚设。失败方式随层固定，禁止层内混用。

---

## 附录 A　上线前必办事项

第 2 章落地时已将下列配置从 `application*.yml` 迁出，其权威取值改由 Nacos 下发。**代码中的兜底默认值一律为安全侧取值，与生产实际值不同**，因此部署前必须确认目标环境的 Nacos 已包含这些键，否则服务会以保守取值运行。

以 `config/nacos/trip-booking-spa.yaml.example` 为准核对：

| 键 | 缺失时的实际行为 | 影响 |
|---|---|---|
| `ratelimit.qps`<br>`ratelimit.default-qps` | 全部接口回落至 `default-qps` 代码默认值 10 | 各接口限速收紧（原 Expedia 查价 50 → 10），静态摄取与查价变慢，但不会超限 |
| `expedia.query.price.queue.task.{switch,return}` | 回落至 `0` | Expedia 刷价任务不执行，价格缓存不再更新 |
| `ratehawk.query.price.queue.task.{switch,return}` | 回落至 `0` | RateHawk 刷价任务不执行 |
| `query.cache.supplier`<br>`supplier.hotel.cache.map`<br>`supplier.query.timezone` | 回落至空 | 查价全部走实时，不读缓存 |

> 均为保守方向的降级，不会造成超发或超限；但**刷价与缓存会静默停摆**，需在部署后核对。

另：现有 dev / prod Nacos 中的 `expedia.commission.rate` 为死配置，代码零引用（佣金取自 Expedia 响应的 `marketing_fee`），应当一并删除。
