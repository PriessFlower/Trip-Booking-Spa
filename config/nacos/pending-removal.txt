# 生产 Nacos 里还留着、但仓库里已经没有读取方的键。
#
# 它们不算「Nacos 有文件无」的漂移失败（那条规则的本意是"生产在跑的键必须被记录"，
# 而这些键并没有在跑），但必须登记在案：写清为什么，清掉后把整段删掉。
#
# ⚠️ 格式：**一行一个键，说明一律写成 # 注释**。本文件按行解析（去掉 # 之后仍非空的行
# 即视为一个键名），所以缩进的说明文字会被当成键，进而在「已登记为待清理，但 Nacos 里
# 已经没有它了」这条上把部署卡住。2026-09-14 就是这么挂掉一次生产部署的。
# 键一旦真的从 Nacos 消失，check-nacos-key-drift.py 会要求删掉对应行——
# 故本清单不会腐烂成一张永久豁免名单。
#
# ⚠️ 删 Nacos 键的时机：必须等【删掉读取方的那个版本真正上了生产】之后。
# 生产跑的镜像可能落后于 dev；提前删键，线上仍在读它的旧版本会静默落回代码兜底值，
# 等于在无人预期的时刻改变行为。2026-08-19 实证：先删了
# task.timezone-init.suppliers（dev 上读取方已删、生产镜像上还在），线上立刻落回空值
# 并打出 "Nacos config ... is empty or invalid"，随即恢复原值。

# ── supplier.fliggy.booking-enabled ─────────────────────────────────────────
# 原因：飞猪下单安全护栏已迁到 application.yml 的 fliggy.booking-enabled。§3.2.3 要求
#   护栏必须经发版与评审才能改，而本键在 Nacos 配置台上点一下即改；且 FliggyProperties
#   是 @ConfigurationProperties，Spring Cloud 的重绑定会让那一下热生效（2026-09-14 实测：
#   改完 Nacos，运行中的进程当场重打配置日志、取值翻转，全程未重启）。
# 起因：2026-09-09 有人为试真单把它点开，注释写着「测完关回 false」，到 2026-09-14 被发现
#   时已连续开了五天。飞猪没有沙箱，开着就是真扣钱。2026-09-14 已在 Nacos 改回 false。
# 代码侧现状：读取方已移出，本键在 FliggyProperties 上无属性可匹配、被静默忽略，不影响
#   启动；只要它还在 Nacos 里，每次启动会打一条 [gate] error 提示清理。
# 删除时机：等含该改动的版本真正上了生产之后。
supplier.fliggy.booking-enabled
