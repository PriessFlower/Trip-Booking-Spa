# 生产 Nacos 里还留着、但仓库里已经没有读取方的键。
#
# 它们不算「Nacos 有文件无」的漂移失败（那条规则的本意是"生产在跑的键必须被记录"，
# 而这些键并没有在跑），但必须登记在案：每行写清为什么，清掉后把行删掉。
# 键一旦真的从 Nacos 消失，check-nacos-key-drift.py 会要求删掉对应行——
# 故本清单不会腐烂成一张永久豁免名单。
#
# ⚠️ 删 Nacos 键的时机：必须等【删掉读取方的那个版本真正上了生产】之后。
# 生产跑的镜像可能落后于 dev；提前删键，线上仍在读它的旧版本会静默落回代码兜底值，
# 等于在无人预期的时刻改变行为。2026-08-19 实证：先删了
# task.timezone-init.suppliers（dev 上读取方已删、生产镜像上还在），线上立刻落回空值
# 并打出 "Nacos config ... is empty or invalid"，随即恢复原值。

# —— 以下三条属于本次限流器统一（2026-08-25）的过渡期，发版落地后逐条清理 ——

# 读取方已随「统一成一个分布式限流器」删除：不再有 local/distributed 开关。
# 生产此刻跑的仍是旧镜像、还在读它，故不能现在删。
ratelimit.mode

# 读取方已删：Expedia 刷价的速率改为从 ratelimit.qps 的用途桶取，不再单独配一份，
# 避免"同一件事两个开关"。同上，等新镜像上了生产再删。
task.expedia-cps.qps

# 与上面两条性质不同，这条【不是死键】：生产 Nacos 里它还是一整行 JSON 字符串，
# 而新代码把 ratelimit.qps 当 YAML map 读，同时保留了读 JSON 的过渡兜底，所以新镜像
# 靠这条也能拿到正确配额。
#
# 登记在此只为解开一个死结：闸要求"Nacos 在跑的键必须在 example 里"，而 example 已经
# 是 map 形态（YAML 里同一个键不可能既是标量又是 map），于是这个标量叶子必然对不上。
#
# 顺序必须是【先发版、后转 Nacos】，不能反：先把 Nacos 转成 map，线上旧镜像的
# @Value 取不到字符串会整体落回 default-qps=20，而艺龙 detail 只有 15 —— 那是在
# 无人预期的时刻对供应商超速。反过来先发版则全程有正确配额，转换窗口内零超速。
# 转成 map 之后这个标量叶子就消失了，届时闸会要求删掉本行。
ratelimit.qps
