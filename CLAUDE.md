# CLAUDE.md

本文件为 Claude Code（claude.ai/code）在本仓库工作时的指引。

## 写完代码后，必须读 PROJECT.md

**`PROJECT.md` 是本仓的开发规范，不是背景介绍。代码写完、宣布完成之前，必须回头对照它自查一遍。**

怎么查：

- **只读与本次改动相关的章节**，不必通读。章节：§1 提交规范 / §2 分支与发布 / §3 配置规范 / §4 架构纪律 / §5 文档规范 / §6 日志规范。但"相关"是需要判断的，拿不准就读——漏掉一章的代价通常是返工。
- **看清措辞强度**（§0.1）：**必须 / 禁止** 是强制条款，违反视为缺陷，评审应当拒绝；**应当 / 不应** 是推荐条款，不遵循必须在 PR 里说明理由；**可以** 由实施者自行判断。
- **发现改动违反了某条，改代码，不要改条款。** 条款号已被代码注释与提交信息大量引用，等同于标识符（§0.2）。确实认为条款本身有问题的，单独提出来讨论；即便要改也只能追加新编号，禁止重排既有编号。
- **汇报时引用条款号**，例如「已按 §4.2.3 保留 wire 字面量原名」。说不出对照了哪几条，等同于没查。

## PROJECT.md 不覆盖的部分

PROJECT.md 有意不复制这些内容，以免两处维护漂移：

- **层的定义、职责边界与各层禁令** —— 以 `docs/architecture.md` §2 为唯一出处（§4.1.1）。PROJECT.md §4.1.3 只给"换一家供应商要不要改"的归属判据表。
- **刷价设计的规范条款** —— 在 `docs/price-refresh.md`，用 `F-x.y` 编号，强度措辞同 §0.1。
- **配置项与生产 Nacos 的对应** —— 见 PROJECT.md 附录 A，`config/nacos/trip-booking-spa.yaml.example` 必须与生产 Nacos 保持同步。
