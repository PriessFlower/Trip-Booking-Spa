server:
  port: 8089
spring:
  config:
    import: optional:nacos:trip-booking-spa.yaml
  # 基础设施连接一律由环境变量注入，与 prod 同形，仓库内不落任何地址与口令。
  # 原先写死的是旧中台库 intl_bg_admin（8.146.211.133，账号 bystest）——项目已迁至
  # spa 专属库 tg_trip_spa（docs/legacy-schema-restoration.md），该配置系改名迁移时
  # 遗留，指向的是被本项目替换掉的那套系统。
  # 无默认值系有意为之：未注入即启动失败，好过静默连上旧库。
  # 本地开发用 dev,local，datasource 与 redis 由 local 剖面覆盖，不受此处影响。
  datasource:
    host: ${DB_HOST}
    port: ${DB_PORT:3306}
    name: ${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${spring.datasource.host}:${spring.datasource.port}/${spring.datasource.name}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true&allowMultiQueries = true&nullCatalogMeansCurrent=true&serverTimezone=CST&autoReconnect=true
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    lettuce:
      pool:
        max-active: 8
        max-idle: 32
        min-idle: 4
  cloud:
    nacos:
      discovery:
        # 保留本地默认：非机密，且 local 剖面不覆盖此键，去掉默认值会让 dev,local 起不来
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
#        group: DEV      # 配置分组
      config:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        file-extension: yaml         # 配置文件格式
        prefix: trip-booking-spa      # 配置前缀，通常是应用名
    inetutils:
      preferred-networks: 192.168.111.198


management:
  endpoints:
    web:
      exposure:
        include: prometheus

# rocketmq：pom 无该依赖、代码零引用，本段为改名迁移遗留的死配置。
# 暂按要求保留，仅去掉写死的地址。
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:}
  producer:
    # 发送同一类消息的设置为同一个group，保证唯一
    group: springboot_producer_group
    # 发送消息超时时间，默认3000
    sendMessageTimeout: 10000
    # 发送消息失败重试次数，默认2
    retryTimesWhenSendFailed: 2
    # 异步消息重试此处，默认2
    retryTimesWhenSendAsyncFailed: 2
    # 消息最大长度，默认1024 * 1024 * 4(默认4M)
    maxMessageSize: 4096
    # 压缩消息阈值，默认4k(1024 * 4)
    compressMessageBodyThreshold: 4096
    # 是否在内部发送失败时重试另一个broker，默认false
    retryNextServer: false

    topic: test
    #sync tag(同步消息tag)
    sync-tag: sync-tags
    #async tag(异步消息tag)
    async-tag: async-tags
    #oneway tag(单向消息tag)
    oneway-tag: oneway-tags

  consumer:
    group: test
    #监听主题
    topic: test
    # 一次拉取消息最大值，注意是拉取消息的最大值而非消费最大值
    pull-batch-size: 10
  hotel:
    group: hotel_consumer_group
    topic: hotel_order_topic
  room:
    group: room_consumer_group
    topic: room_order_topic
  product:
    group: product_consumer_group
    topic: product_order_topic

  enhance:
    # 启动隔离，用于激活配置类EnvironmentIsolationConfig
    # 启动后会自动在topic上拼接激活的配置文件，达到自动隔离的效果
    enabledIsolation: true
    # 隔离环境名称，拼接到topic后，topic_dev，默认空字符串
    environment: dev
expedia:
  apiKey: ${EXPEDIA_API_KEY:}
  sharedSecret: ${EXPEDIA_SHARED_SECRET:}
  session: ${EXPEDIA_CUSTOMER_SESSION_ID:trip-booking-spa-dev}
  ownIp: ${EXPEDIA_CUSTOMER_IP:127.0.0.1}
  localFilePath: /home/webrun/spa/logs/file
  # Tripbooking AI 新合同 B2B profile（2026-08 technical workshop p6）
  # 四项构成一条车道，必须整体切换；混用会静默拿到另一条车道的报价，
  # 故 ExpediaContractProfile 在启动期校验，凑不成已知 profile 即拒绝启动。
  partner_point_of_sale: ${EXPEDIA_PARTNER_POS:B2B_SA_PKG_MOD_AGENT}
  payment_terms: ${EXPEDIA_PAYMENT_TERMS:2}
  billing_terms: ${EXPEDIA_BILLING_TERMS:EAC}
  sales_channel: ${EXPEDIA_SALES_CHANNEL:agent_tool}
  url:
    host: ${EXPEDIA_API_HOST:https://test.ean.com}
system:
  localFilePath: /home/webrun/spa/logs/file/
